package com.app.my_app.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebMvcConfig {
    // Đã dọn dẹp cấu hình cũ gây xung đột với các đường dẫn /api
}